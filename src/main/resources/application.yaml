spring:
  application:
    name: babysense_server
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update # 테이블이 없으면 자동으로 만들어줌
    show-sql: true
    properties:
      hibernate:
        format_sql: true